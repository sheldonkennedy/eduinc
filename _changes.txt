1) Remove space after rand sign.
2) Add comma as thousand separator, period as rand/cent separator “R1,000.00.”
3) Can we set the default total to format as R0.00 instead of just 0?
4) The disabled button state is class="button small disabled”.
5) Can we hide <table class="feesresult"> until Calculate is clicked? Reset should also hide it.